class Insertion{
  public static void main(String[]args){
    int []arr={9,8,4,5,6,3,7};
    int n=arr.length;
    for(int i=1;i<n;i++){
      int val=arr[i];
      int j=i-1;
      while(j>=0&& arr [j]>val){
        arr[j+1]=arr[j];
        j--;
      }
      arr[j+1]=val;
    }
    for(int i=0;i<n;i++){
      System.out.println(arr[i]);
    }
  }
}

